{
	"name": "server",
	"version": "0.0.1",
	"scripts": {
		"dev": "bun run --hot src/index.ts",
		"deploy": "orbiter deploy --server"
	},
	"dependencies": {
		"hono": "^4.7.7",
		"shared": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "latest"
	}
}
